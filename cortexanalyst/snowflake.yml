definition_version: '2'
entities:
  cortexanalyst_app:
    type: streamlit
    identifier:
      name: cortexanalyst_app
    main_file: streamlit_app.py
    pages_dir: pages
    query_warehouse: small_warehouse
    stage: cortexanalyst_stage
    artifacts:
      - streamlit_app.py
      - environment.yml
      - pages
      - common/hello.py
      - call_center_analytics.yaml

